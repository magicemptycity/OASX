# testoyj 更新日志

本文件只记录 `XDeolx/OASX` 的 `testoyj` 分支改动，独立于上游 `CHANGELOG.md`、README 和许可证。

当前版本：`testoyj-v1.0.10`

## 2026-09-04

### 爬塔结算专项分析

- 行为分析新增独立的“爬塔结算专项分析”，按新版 OAS 的结构化日志分别统计任务级区域模板、结算场次、加权点击、查看详情与随机快速结算，不再需要从全局点击中手动辨认。
- 新增 A-E 区域实际占比与目标权重对照，并提供只包含 `CLIMB_SETTLEMENT_*` 点击的坐标连线图；模板、详情和快速点击均保留各自事件类型。
- “查看详情”停留和“快速结算”点击间隔使用单独的随机等待图，仍可与整日总览和任务筛选配合查看；旧日志没有专项标记时保持原有分析界面。

### 爬塔行为分析

- 行为分析的随机等待图新增识别爬塔疲劳模式：战前延迟、结算延迟和周期休息都会按日志时间归入当前爬塔任务，可通过任务筛选单独查看。
- 兼容 OAS 现有的英文爬塔日志和分钟级休息记录；长休息在图表提示中会显示为分钟和秒，不再遗漏。

### 爬塔配置适配

- 适配 OAS 新增的门票困难/简单模式和体力爬塔五倍券配置；更新 OAS 后重新读取即可在 OASX 中编辑这些参数。

## 2026-09-03

### Windows 内置更新启动修复

- 修复部分 Windows 环境中更新包下载完成后仅显示“正在准备安装更新”、随后无后续动作的问题；更新器改为可确认启动的方式运行，避免完全脱离式启动被系统静默拦截。
- 更新器在准备阶段意外退出时会立即显示退出码，不再等待三分钟后才超时；更新过程仍在准备完成并写入交接标记后才关闭 OASX。

### 周刷新

- 周计划新增默认关闭的“周刷新”开关、设置入口和立即刷新按钮；每周实际时间与基础计划分开显示和保存，避免随机结果逐周累积。
- 设置窗口支持 10–20 分钟默认秒级波动、固定任务排除、冻结时段管理，以及按具体星期计划项设置最早和最晚时间边界。
- 新增刷新预览，保存前可逐日检查“基础时间 → 本周时间”及无合法候选冲突；任务可以交换先后，但生成结果绝不会超出各自边界。
- 任务边界新增“统一设置 / 单独设置”切换：统一设置可一次写入同一任务的所有已计划星期，单独设置可覆盖其中某一天。
- 周刷新设置窗口新增“保存当前设置”，保存后窗口不关闭；旧版 OAS 会明确提示需先更新后端，不再将被忽略的保存误报为成功。

### Windows 内置更新

- 重写更新交接方式，安装路径改用 JSON 清单传递，兼容用户名或目录中的空格、单引号等特殊字符。
- 更新包解压、安装目录和写入权限会在 OASX 退出前完成校验；替换或新版启动失败时自动恢复并重启旧版。

## 2026-09-01

### Windows 内置更新交接加固

- 更新路径改由 JSON 清单传给 PowerShell，不再经过批处理命令拼接，兼容用户名或安装路径中的空格、单引号和其他特殊字符。
- 更新器会先检查安装目录、当前程序、更新包和目录写权限，完成解压校验与恢复副本后才让 OASX 退出；准备失败时 OASX 保持运行并显示具体原因。
- 文件替换改用 Windows 路径 API，并记录实际安装路径、系统与 PowerShell 版本；替换或新版启动失败时会恢复并重新启动旧版。

### 行为分析任务筛选

- 日期选择旁新增任务筛选，可查看全部任务或单独选择一个任务；点击位置与连线、随机等待、点击按压时长、顶部统计和任务时间轴会同步筛选。
- 临时版本号使用 `testoyj-v1.0.4.1`，确保现有 `v1.0.4` 可通过内置更新升级；后续正式版本继续使用三段式版本号。

### 本地行为分析

- 在首页“日志、统计”旁新增“行为分析”，按当前配置和日期直接读取 OAS 本地日志；数据不关联游戏账号、不联网上传，一份配置切换多个账号时展示该配置日志的整体情况。
- 新增全部点击位置与顺序连线、随机等待分布和各任务点击按压时长分布；图表支持鼠标悬停查看具体时间、任务、点击目标、坐标、等待时长、区间数量与占比。
- 新增任务与重启时间轴：不同任务用不同颜色的粗线段表示启动至结束区间，缺少结束记录时由下一任务或当天最后日志时间推断并明确标注。
- 异常重启、任务运行和脚本生命周期分为三条纵向轨道；计划内正常重启使用循环箭头，脚本启动使用朝右小车，非计划应用重启与 ATX 重启使用红色异常图标，并可悬停查看原因备注。

### OAS 重读自动刷新

- OAS 后端重启并恢复连接，或用户手动刷新 OAS 后，OASX 会自动统一重新读取配置列表、任务菜单、当前任务参数、周计划和远程翻译。
- 后端配置与翻译请求会跳过旧缓存；OAS 更新后不再需要关闭并重开 OASX 才能看到新内容。
- 刷新后主动重新获取各配置的调度状态，避免界面仍显示重读前的任务时间。

### 周计划覆盖情况固定显示

- 保留原有覆盖情况的内容与样式，将其固定在任务列表上方，不再随任务列表滚动隐藏。

## 2026-08-31

### 周计划批量波动精度

- 批量添加任务的随机浮动由整分钟抽样改为秒级抽样；例如设置 5–10 分钟时，每个所选星期会独立生成位于 300–600 秒范围内的正负偏移。
- 保持随机结果写入后固定、不同星期尽量使用不同时间以及不跨到相邻日期的原有规则；波动设为 0–0 分钟时仍保留基准时间。

### 更新检查限流回退

- GitHub API 返回 `403/429` 或连接失败时，自动改用普通 Release 跳转地址识别最新版本。
- 备用通道不消耗 GitHub API 匿名额度，并可继续定位和下载对应的 Windows ZIP 更新包。

### 首个正式版本

- 版本号升级为 `testoyj-v1.0.0`，作为 testoyj OASX 的首个正式版本。
- 内置更新兼容从 `testoyj-v0.3.12.9` 直接升级至 `testoyj-v1.0.0`，后续采用标准三段版本号。

### 百鬼棋局测试版提示

- 将新版 Chess 的中文任务名显示为“百鬼棋局（测试版）”，远程翻译尚未载入时也可使用内置中文名称。
- 在任务参数页顶部显示测试提示；如运行不稳定，可按提示将 OAS 切换到 `testoyj-chess-legacy` 分支使用旧版。
- 保护测试版名称和回退提示，连接仍返回旧翻译的 OAS 时也不会重新显示成不带标记的“百鬼棋局”。

## 2026-08-30

### 在线更新检查修复

- 更新检查改用独立的 GitHub 请求，不再经过 OAS 本地服务的响应包装与 3 秒连接超时。
- 为 GitHub API 补充请求头，并将连接、响应超时分别调整为 15 秒和 20 秒，降低网络波动导致的误报。
- Release 数据缺少版本或页面地址时会返回明确的格式错误，不再只显示没有原因的“检查更新失败”。
- 已完成从 `testoyj-v0.3.12.8` 在线更新至 `testoyj-v0.3.12.9` 的实机验证，版本检查、下载、替换与重启链路均可正常工作。

### 周计划窄窗口布局

- 缩小“周计划启用”和“龟缩模式”开关，降低顶部状态区的横向占用。
- 将“周计划启用、补跑今日已过计划”和“龟缩模式、保留任务、自由循环”分成两行，开关、标签与选择按钮保持成组显示。
- 操作按钮改为独立工具栏；窗口宽度不足时整体换到下一行右侧，不再把状态文字挤散。

### 应用内更新

- 更新器改为检查 `XDeolx/OASX` 的最新 Release，可在设置中随时手动检查，并且每天自动检查一次。
- Windows 版支持在 OASX 内下载 ZIP、校验文件、替换程序并重新启动；新版本启动失败时会自动恢复旧版。
- 修正 `testoyj-v0.3.12.5` 版本标签和构建号识别，更新完成后不会反复提示同一版本。

### 结界蹭卡参数适配

- 为“顺手进行寮抽奖”和“寮奖励随机等待”补充中文标题及详细说明，不再显示英文配置键名。
- 明确资金和体力始终在蹭卡收尾时顺手收取，寮抽奖开关只控制抽奖行为。
- 明确寮抽奖与随机等待均默认关闭；随机等待开启后会在寮资金、体力和抽奖动作之间等待 `2-4` 秒。

## 2026-08-29

### 周计划批量编排

- 支持一次选择任务、基准时间和多个星期，批量写入一周计划。
- 支持按基准时间前后随机浮动 0–60 分钟；随机结果在添加时固定，后续同步不会重新随机。
- 默认选择周一至周日，并尽量为各天生成不同时间；临近零点时不会把计划跨到相邻日期。
- 新增“覆盖所选星期中已存在的同名任务”选项，只替换对应星期的同名任务，不影响其他任务和未选星期。

### 周计划时间选择器

- 默认使用 12 小时表盘，先选择 AM 或 PM，再依次拨动时针、分针和秒针。
- 表盘同时显示三根指针，当前正在调整的指针高亮；点击上方时、分、秒数值可随时切换。
- 保留键盘输入模式，可通过右上角键盘或时钟图标切换。
- 周计划新增秒级保存和显示，并兼容原有仅包含时、分的计划记录。

## 2026-08-27

### 龟缩模式

- 切换龟缩模式或修改保留列表时使用保留时间同步，不再触发主调度器任务时间重排；模式开启后盾牌显示为浅蓝色。
- 周计划新增龟缩模式开关和保留任务选择器，默认推荐保留结界蹭卡、结界挂卡和地域鬼王。
- 开启后只显示保留任务的计划，并立即停用主调度器中的其他任务；隐藏记录不会被删除。
- 关闭龟缩模式后恢复完整周计划，OAS 会重新启用计划内任务并保留各任务当前的下一次运行时间。
- 停用周计划时若龟缩模式仍开启，会先执行任务恢复，避免其他任务长期处于停用状态。

### 自由循环任务

- 新增与龟缩模式一致的任务选择器，默认选择结界挂卡和结界蹭卡，可按需增减或全部清空。
- 自由循环任务在零点自动同步时延续主调度器已有时间，手动“立即同步今日计划”仍可强制校时。
- 顶部工具栏与任务摘要显示循环图标，当前集合非空时使用浅蓝色状态。

### 结界蹭卡配置

- 增加怠惰模式、触发概率和好友分组回退的配置及中文说明。
- 蹭卡收尾只单次处理寮主页可见奖励；寮抽奖保持可选，不会在默认配置下自动执行。

## 2026-08-26

### 周计划页面

- 在配置工作区新增独立的“周计划”标签页。
- 支持添加、编辑和删除任务计划，并可按全部、周一至周日筛选查看。
- 支持启用或停用周计划，并显示 OAS 服务端当前时间、本周周一和今日最后同步时间。
- 每条计划同时显示星期、时间和按当前周换算后的实际日期，避免复制计划后日期与星期不一致。
- 支持将某一天的整套计划复制到另一天，复制后按照目标星期重新计算实际日期。
- 支持从当前主调度器整体导入已启用任务，可选择先清空现有周计划条目。
- “从当前调度器导入”只复制数据到周计划，不会清空或停用 OAS 主调度器。
- 支持“补跑今日已过计划”选项，以及“立即同步今日计划”操作。
- 新增计划内和未计划任务列表，便于检查哪些任务受周计划管理。

### 界面修复

- 修复周计划条目区域在窄窗口和较长状态文本下的横向溢出。
- 限制状态标签宽度并优化日期布局，改善 Windows 桌面端显示。
- 修复周计划相关组件测试和静态分析问题。

### Windows 构建

- 新增 `testoyj` 分支专用的 Windows GitHub Actions 构建流程。
- 构建流程执行 Flutter 分析、测试和 Windows 打包，并上传测试构建产物。
- `testoyj-v*` 版本标签会创建 GitHub Release，并附带带版本号的 Windows ZIP 安装包。
- `testoyj` 不部署独立 Web 版本，README 中的 Web 链接仅指向上游在线版。
- 优化 Actions 错误输出，使分析或测试失败时能够直接看到有效诊断。

### 兼容说明

- 周计划功能需要配套使用 `XDeolx/OnmyojiAutoScript` 的 `testoyj` 分支；仅更新 OASX 无法获得后端周计划接口。
- 周计划启用后由 OAS 每日自动同步，不需要每天重新执行“从当前调度器导入”。
- 默认同步行为不会清空主调度器；任务执行后的成功间隔和失败重试继续由 OAS 调度器维护。
- OASX 构建包不包含本机的 OAS 账号配置文件。

## 参考说明

- `testoyj` 的周计划、龟缩模式、自由循环和批量编排为本分支功能；基础界面建立在 OASX 项目之上。
- 上游项目：<https://github.com/AzurTian/OASX>
- 分支仓库：<https://github.com/XDeolx/OASX/tree/testoyj>
- 配套 OAS 更新日志：<https://github.com/XDeolx/OnmyojiAutoScript/blob/testoyj/TESTOYJ_CHANGELOG.md>
